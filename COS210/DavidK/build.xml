<project name="BigGame" default="compile">
    <property name="src"     location="src"/>
    <property name="build"   location="build"/>
    <property name="classes" location="${build}/classes"/>
    <property name="dist"    location="dist"/>
    <property name="classpath"  value="${build}/jar/${ant.project.name}}.jar:.:/home/dk/Documents/COS210:vecmath.jar:${classes}" />

    <target name="clean">
        <delete dir="${build}"/>
    </target>

    <target name="compile">
        <mkdir dir="${classes}"/>
        <javac srcdir="${src}" destdir="${classes}" debug="true">
            <!--
            <compilerarg value="-Xlint:all"/>
            <compilerarg value="-Xlint"/>
            -->
        </javac>
    </target>

    <target name="jar" depends="compile">
        <mkdir dir="${build}/jar"/>
        <jar destfile="${build}/jar/${ant.project.name}}.jar" basedir="${classes}">
            <manifest>
                <attribute name="Main-Class" value="NBody"/>
            </manifest>
        </jar>
    </target>

    <target name="run" depends="compile">
        <java 
            classname="edu.mccc.cos210.biggame.BigGame"
            classpath="${classpath}"
            fork="true" >
            <arg value="MommasBadBoy" />
        </java>
    </target>

    <target name="rund" depends="compile">
        <java 
            classname="edu.mccc.cos210.biggame.BigGame"
            classpath="${classpath}"
            fork="true" >
            <arg value="MommasBadBoy" />

            <sysproperty 
                key= "com.cbthinkx.debug" 
                value= "true" />
            <!--
            <arg value="-Dcom.cbthinkx.debug" />
            -->
        </java>
    </target>

    
    <target name="server" depends="compile">
        <java 
            classname="edu.mccc.cos210.biggame.BigGameServer"
            classpath="${classpath}"
            fork="true"
        />
    </target>

    <target name="serverd" depends="compile">
        <java 
            classname="edu.mccc.cos210.biggame.BigGameServer"
            classpath="${classpath}"
            fork="true"
            >

            <sysproperty 
                key= "com.cbthinkx.debug" 
                value= "true" 
            />
        </java>
    </target>


</project>
